#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
    int a, b, x;

    // Input
    cin >> a >> b;

    // Processing
    x = a + b;

    // Output
    cout << "X = " << x << endl;

    return 0;
}
